export function getCliTuiRoadmap(): string[] {
  return [
    'Smart CLI commands for init, doctor, run, memory-plan, and channel operations',
    'Interactive terminal chat loop with streaming-friendly output',
    'Keyboard-driven TUI for sessions, memory inspection, and channel monitoring',
    'Plugin-aware message composer for WhatsApp first, with future Slack, Discord, and iMessage support',
    'Built-in diagnostics and setup wizard for local and hosted deployments',
  ];
}
